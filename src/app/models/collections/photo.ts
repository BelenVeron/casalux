export class Photo {
    id: string;
    image: string; 
    

    public constructor( 
        id: string,
        image: string,
        ) {
            this.id = id;
            this.image = image;
    }
}
